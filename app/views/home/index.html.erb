<h1>Current Match Threads</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Kickoff</th>
  </tr>
 
<% @match_threads.each do |thread| %>
  <tr>
    <td><%= link_to thread.title, matchthread_path(thread) %></td>
    <td><%= thread.author %></td>
    <td><%= thread.id %></td>
  </tr>
<% end %>
</table>
 
<br>
